import calculate                #  同層  

print(calculate.add(5,2))
print(calculate.sub(5,2))

from calculate import add, sub

from calculate import*

from calculate import add as a 

import calculate as cal 

# 在別的專案 使用 自己的模組   放預設 模組  位置 即可